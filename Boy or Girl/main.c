// https://codeforces.com/problemset/problem/236/A

#include <stdio.h>

int check_gender(char *);
int count_distinct_characters(char *);
int count_character(char *, char);
char *get_distinct_characters(char *);

int main(int argc, char const *argv[])
{

    return 0;
}

int count_character(char *string, char c)
{
    int counter = 0;

    return counter;
}

int count_distinct_characters(char *username)
{
    int counter = strlen(username);
    for (char *pointer = username; *pointer != '\0'; pointer++)
    {
    }
}
